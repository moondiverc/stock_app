import 'package:flutter/material.dart';

class AppPallete {
  static const Color themeColor = Color.fromRGBO(85, 51, 187, 1.0);
  static const Color backgroundColor = Color.fromRGBO(255, 255, 255, 1);
  static const Color errorColor = Colors.redAccent;
  static const Color textColor = Color.fromRGBO(74, 74, 74, 1.0);
  static const Color backgroundChipColor = Color.fromRGBO(74, 74, 74, 1.0);

  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;

  static const Color gainColor = Colors.green;
  static const Color lossColor = Colors.red;

  // news sentiment color
  static const Color sentimentBullish = Color.fromRGBO(200, 230, 201, 1);
  static const Color sentimentBearish = Color.fromRGBO(255, 205, 210, 1);
  static const Color sentimentNeutral = Color.fromRGBO(234, 234, 238, 1);

  static const Color sentimentBullishText = Color.fromRGBO(46, 125, 50, 1.0);
  static const Color sentimentBearishText = Color.fromRGBO(198, 40, 40, 1.0);
  static const Color sentimentNeutralText = Color.fromRGBO(58, 58, 60, 1.0);

  static const Color transparentColor = Colors.transparent;
  static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
  static const Color gradient1 = Color.fromRGBO(187, 132, 251, 1);
}
